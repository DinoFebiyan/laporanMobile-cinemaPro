= Bukti Keaslian Kode (Strict Mode)

Sesuai instruksi ujian untuk mencegah penggunaan *code generator* otomatis    //[cite: 8].

== Watermark Code
Berikut adalah bukti penggunaan suffix inisial pada variabel Widget dan Fungsi:

// Ganti dengan screenshot kode Anda
#image("../images/watermarkContoh.png", width: 80%)
*Penjelasan:* Fungsi `_formatPrice_dino` menunjukkan kode dibuat oleh Dino (UI Engineer)  //[cite: 9].

== Logic Trap
Implementasi logika bisnis "The ”Long Title” Tax"    //[cite: 10].

```dart
// Contoh snippet kode (Logic Trap)
void hitungDiskon_dani(String nim) {
  int lastDigit = int.parse(nim.characters.last);
  if (lastDigit % 2 != 0) {
    // Logika Ganjil
    print("Diskon 5%");
  } else {
    // Logika Genap
    print("Gratis Ongkir");
  }
}

TEMPEL KODEMU NDEK KENE @ISAL
```

Implementasi logika bisnis unik "Odd/Even Seat Rule:"   

```dart
// Contoh snippet kode (Logic Trap)
void hitungDiskon_dani(String nim) {
  int lastDigit = int.parse(nim.characters.last);
  if (lastDigit % 2 != 0) {
    // Logika Ganjil
    print("Diskon 5%");
  } else {
    // Logika Genap
    print("Gratis Ongkir");
  }
}

TEMPEL KODEMU NDEK KENE @ISAL
```